{
  "name": "@arsv2/service",
  "version": "2.0.0",
  "private": true,
  "description": "Layer 3: Business logic for ARSV2 (reorder engine, inventory analysis)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@arsv2/types": "*",
    "@arsv2/config": "*",
    "@arsv2/repo": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
