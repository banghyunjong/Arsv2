{
  "buildCommand": "cd ../.. && npm run build && cp -r packages/ui/dist/* packages/runtime/public/ && mkdir -p packages/runtime/docs && cp docs/*.csv packages/runtime/docs/",
  "installCommand": "npm install --prefix=../..",
  "functions": {
    "api/index.js": {
      "includeFiles": "docs/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
